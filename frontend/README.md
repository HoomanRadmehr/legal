# Frontend implementation target

Codex will create the React application here through the frontend tasks.
The frontend consumes the versioned backend API and listens for user-scoped realtime events.

Expected shape after foundation tasks:

```text
frontend/
├── AGENTS.md
├── package.json
├── lockfile
├── vite.config.ts
├── src/
│   ├── app/
│   ├── api/
│   ├── components/
│   ├── features/
│   ├── i18n/
│   └── test/
└── public/
```

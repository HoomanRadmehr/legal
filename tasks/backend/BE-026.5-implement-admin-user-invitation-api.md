# BE-026.5: Implement Admin user invitation API

Status: DONE
Priority: P0
Area: Backend
Related specs: BE-001, BE-002, BE-008, BE-012, BE-013
Depends on: BE-007, BE-008, BE-009, BE-011, BE-018, BE-019

## Goal

Allow a Legal Admin to create a new organization user securely without choosing the user's password.

The operation creates:

- an inactive User,
- an organization Membership,
- a pending UserInvitation,
- an append-only activity record,
- a transactional outbox event for the invitation email.

The organization is always derived from the authenticated Admin's active membership.

## Business rules

1. Only an active `legal_admin` membership may create organization users.
2. The request must never accept `organization_id`.
3. The Admin must not provide a password.
4. The new User is initially created with:
   - `is_active=False`,
   - an unusable password,
   - normalized email,
   - canonical preferred language.
5. The Membership uses one of:
   - `legal_admin`,
   - `legal_manager`,
   - `legal_counsel`,
   - `viewer`.
6. The Membership belongs to the Admin's current organization.
7. The Membership may use the existing `active` status because the inactive User cannot authenticate or appear in active-user selectors.
8. Owner and assignee selectors must continue to require an active User and active Membership.
9. Duplicate email addresses return a stable `409` error.
10. No invitation token is stored in plaintext.
11. No invitation URL or token is written to application logs, audit metadata, or outbox payloads.
12. Invitation email is a mandatory transactional email and does not depend on user notification preferences.
13. Hard deletion is not added.
14. The operation supports `Idempotency-Key`.
15. Reusing an idempotency key with different input returns `409`.

## API contract

```text
POST /api/v1/memberships/
```

## Acceptance criteria

- [x] Active Legal Admin can create an inactive user, active organization membership, pending invitation, activity record, and outbox event.
- [x] Non-admin roles cannot create invitations.
- [x] Request payload does not accept `organization_id` or `password`.
- [x] Duplicate email addresses return a stable `409`.
- [x] Invitation token is stored only as a hash, and outbox/activity payloads contain no token, invitation URL, or email address.
- [x] `Idempotency-Key` replay returns the stored safe response, and conflicting reuse returns `409`.
- [x] Active membership selectors continue to exclude inactive invited users.

## Verification commands

```bash
cd backend && python -m pytest apps/organizations/tests -q
```

## Codex execution log

- Started: 2026-07-15 10:45 +0330
- Completed: 2026-07-15 10:45 +0330
- Files changed:
  - `backend/apps/organizations/models.py`
  - `backend/apps/organizations/migrations/0002_userinvitation.py`
  - `backend/apps/organizations/services.py`
  - `backend/apps/organizations/api/v1/__init__.py`
  - `backend/apps/organizations/api/v1/openapi.py`
  - `backend/apps/organizations/api/v1/serializers.py`
  - `backend/apps/organizations/api/v1/urls.py`
  - `backend/apps/organizations/api/v1/viewsets.py`
  - `backend/apps/organizations/tests/factories.py`
  - `backend/apps/organizations/tests/test_invitation_api.py`
  - `backend/apps/organizations/tests/test_migration.py`
  - `backend/apps/organizations/tests/test_selectors.py`
  - `backend/config/urls.py`
  - `tasks/backend/BE-026.5-implement-admin-user-invitation-api.md`
  - `AI_USAGE.md`
- Commands run:
  - `cd backend && python -m pytest apps/organizations/tests -q` (failed before pytest startup because `.python-version` points to uninstalled `3.12`)
  - `cd backend && UV_PROJECT_ENVIRONMENT=/tmp/legal-be0265-venv uv run --python /usr/bin/python3.12 python -m pytest apps/organizations/tests -q`
  - `cd backend && UV_PROJECT_ENVIRONMENT=/tmp/legal-be0265-venv uv run --python /usr/bin/python3.12 ruff check apps/organizations config/urls.py`
  - `cd backend && UV_PROJECT_ENVIRONMENT=/tmp/legal-be0265-venv uv run --python /usr/bin/python3.12 ruff format --check apps/organizations config/urls.py`
  - `python3 scripts/check_simplicity.py backend/apps/organizations backend/config/urls.py`
  - `cd backend && UV_PROJECT_ENVIRONMENT=/tmp/legal-be0265-venv uv run --python /usr/bin/python3.12 python manage.py makemigrations --check --dry-run`
  - `cd backend && UV_PROJECT_ENVIRONMENT=/tmp/legal-be0265-venv uv run --python /usr/bin/python3.12 python manage.py spectacular --file /tmp/legal-be0265-openapi.yaml --validate`
  - `python3 scripts/validate_docs.py`
- Result: Implemented and verified the admin-only user invitation API. Organizations tests passed with 22 tests; lint, format, simplicity, migration drift, and OpenAPI validation passed.
- Deviations/questions: The task file was malformed before implementation: no closing API contract fence, no allowed scope, no acceptance section, no verification section, and no execution log. Root URL routing was added so `POST /api/v1/memberships/` resolves. `python3 scripts/validate_docs.py` still fails because decimal task IDs are not accepted by the validator and unrelated future task references are missing.

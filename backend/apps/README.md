# Backend domain apps

Domain Django apps will be added here by their own task IDs.

Do not generate all domains during foundation scaffolding. Each domain must keep its own models,
selectors, services, API serializers, filters, permissions, OpenAPI declarations, and tests.

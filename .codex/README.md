# Codex prompt helpers

These are reusable prompt fragments, not configuration that grants permissions.
The authoritative agent rules are in the root and nested `AGENTS.md` files.
Use one task prompt per Codex run.

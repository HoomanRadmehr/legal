# Test one task

```text
For <TASK-ID>, read the task acceptance criteria and linked specs. Add the smallest tests
that prove each criterion, prioritizing behavior and security boundaries. Use two organizations
for authorization tests and frozen time for deadline boundaries. Mock external providers only
at explicit function boundaries. Do not change production behavior unless a test exposes a
confirmed bug. Run the task verification commands and report exact results.
```
